const numbers = [1, 2, 3];

// this is a map function
const doubleNumArray = numbers.map((pizza) => {
  return pizza * 2
});

console.log(numbers); // [1, 2, 3]
console.log(doubleNumArray); // [2, 4, 6]
